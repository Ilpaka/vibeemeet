# 🚀 Быстрый старт - Видеоконференция ПК ↔ Телефон

## 📱 Запуск для локальной сети (одной командой)

### Windows (рекомендуется)

```batch
start-local.bat
```

или PowerShell:
```powershell
.\start-local.ps1
```

Скрипт автоматически:
- Определит ваш IP в локальной сети
- Запустит все сервисы
- Покажет URL для телефона

---

## 🎥 Видеоконференция между ПК и телефоном

После запуска вы увидите:
```
Локальный IP: 192.168.0.153
Откройте на телефоне: http://192.168.0.153
```

### Пошаговая инструкция:

1. **На ПК**: откройте http://localhost
2. Нажмите **"Создать комнату"**
3. Введите ваше имя → **Продолжить**
4. **Скопируйте ID комнаты** (появится модальное окно)
5. **На телефоне**: 
   - Подключитесь к той же WiFi сети
   - Откройте http://ВАШ_IP (например http://192.168.0.153)
   - Вставьте ID комнаты → **Войти**
6. 🎉 **Готово!** Вы в конференции!

---

## Ручной запуск (с указанием IP)

### 1. Определите ваш IP

**CMD:**
```batch
ipconfig | findstr "IPv4"
```

**PowerShell:**
```powershell
(Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -like "192.168.*"}).IPAddress
```

### 2. Запустите с указанием IP

**PowerShell:**
```powershell
$env:HOST_IP="192.168.0.153"  # Замените на ваш IP
docker compose up --build -d
```

**CMD:**
```batch
set HOST_IP=192.168.0.153
docker compose up --build -d
```

---

## 📊 Адреса и порты

| Устройство | URL |
|------------|-----|
| ПК (localhost) | http://localhost |
| Телефон / планшет | http://ВАШ_IP |
| LiveKit WebSocket | ws://ВАШ_IP:7880 |

---

## ⚠️ Возможные проблемы

### Телефон не может подключиться

**Проверьте:**
1. ПК и телефон в **одной WiFi сети**
2. Порты открыты в Windows Firewall

**Откройте порты (PowerShell от Администратора):**
```powershell
New-NetFirewallRule -DisplayName "Video Conference HTTP" -Direction Inbound -Port 80 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "Video Conference LiveKit WS" -Direction Inbound -Port 7880 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "Video Conference LiveKit UDP" -Direction Inbound -Port 50000-50100 -Protocol UDP -Action Allow
```

### Видео зависает / не отображается

- Включите/выключите камеру
- Проверьте разрешения браузера на камеру и микрофон
- Проверьте логи: `docker compose logs -f backend`

### "Подключение к комнате..." зависает

- Проверьте что LiveKit работает: откройте http://localhost:7880
- Убедитесь что HOST_IP указан правильно

---

## 🛠 Полезные команды

```bash
# Просмотр логов
docker compose logs -f

# Только backend
docker compose logs -f backend

# Остановка
docker compose down

# Полная очистка (с удалением БД)
docker compose down -v

# Перезапуск с пересборкой
docker compose up --build -d
```

---

## 🔒 Для продакшена

1. Измените секретные ключи в `.env`
2. Настройте HTTPS (SSL сертификат)
3. Используйте внешний TURN сервер для NAT traversal

---

## 📱 Тестирование без телефона

Можно использовать два браузера:
1. Chrome обычный
2. Chrome в режиме инкогнито

Или два разных браузера (Chrome + Firefox).
