# 🚀 Полный гайд по запуску VibeMeet с авторизацией

## Проблема была в том, что Auth-сервис не был запущен!

---

## ⚡ Быстрый запуск (3 терминала)

### Терминал 1: Auth-сервис

```bash
cd /Users/ilpaka/Development/NextUp-website
./start-auth-service.sh
```

Этот скрипт:
- Запустит PostgreSQL на порту 5433
- Запустит Auth-сервис на порту 8080
- Оставьте этот терминал открытым!

### Терминал 2: VibeMeet

```bash
cd /Users/ilpaka/Development/vibeemeet
docker-compose up
```

Или в фоновом режиме:
```bash
docker-compose up -d
```

### Терминал 3: Проверка

```bash
# Проверяем Auth-сервис
curl http://localhost:8080/health
# Должен вернуть: {"status":"ok"}

# Проверяем VibeMeet
curl http://localhost/health
# Должен вернуть статус OK

# Проверяем proxy на auth API
curl -X POST http://localhost/auth-api/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"Test123!@#","first_name":"Test","last_name":"User"}'
```

---

## 📋 Пошаговая инструкция

### Шаг 1: Запустите Auth-сервис

**Важно:** Auth-сервис должен быть запущен ПЕРВЫМ!

```bash
cd /Users/ilpaka/Development/NextUp-website
./start-auth-service.sh
```

Вы должны увидеть:
```
Auth-сервис будет доступен на http://localhost:8080
Для остановки нажмите Ctrl+C
```

Оставьте этот терминал открытым!

### Шаг 2: Запустите VibeMeet

В НОВОМ терминале:

```bash
cd /Users/ilpaka/Development/vibeemeet
docker-compose restart nginx  # Перезапускаем nginx с новой конфигурацией
```

Или полный перезапуск:
```bash
docker-compose down
docker-compose up -d
```

### Шаг 3: Откройте браузер

```
http://localhost
```

### Шаг 4: Зарегистрируйтесь!

1. Нажмите "Регистрация"
2. Заполните форму:
   - Имя: ваше имя
   - Email: ваш email
   - Пароль: минимум 8 символов
   - Подтверждение пароля
3. Нажмите "Зарегистрироваться"

Вы будете автоматически перенаправлены в Dashboard!

---

## 🔍 Проверка работы

### 1. Auth-сервис работает?

```bash
curl http://localhost:8080/health
```

Ожидаемый результат: `{"status":"ok"}`

Если не работает:
- Проверьте, что терминал с `start-auth-service.sh` открыт
- Проверьте, что порт 8080 не занят: `lsof -i :8080`

### 2. VibeMeet работает?

```bash
curl http://localhost/health
```

Ожидаемый результат: статус 200 OK

### 3. Proxy на auth API работает?

Откройте DevTools в браузере (F12) и попробуйте зарегистрироваться.

В Network должен быть запрос: `POST http://localhost/auth-api/register`

**Если 502 Bad Gateway:**
- Auth-сервис не запущен → запустите его!
- Перезапустите nginx: `docker-compose restart nginx`

**Если CORS ошибка:**
- Обновите страницу: Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)

---

## ⚙️ Альтернативный способ (Make)

### Запуск Auth-сервиса через Make

```bash
cd /Users/ilpaka/Development/NextUp-website
make dev
```

Это автоматически:
1. Запустит PostgreSQL
2. Подождет 2 секунды
3. Запустит Auth-сервис

---

## 🛑 Остановка всего

### 1. Остановить Auth-сервис

В терминале с `start-auth-service.sh` нажмите: **Ctrl+C**

Затем остановите PostgreSQL:
```bash
cd /Users/ilpaka/Development/NextUp-website
docker compose down
```

### 2. Остановить VibeMeet

```bash
cd /Users/ilpaka/Development/vibeemeet
docker-compose down
```

---

## 🐛 Troubleshooting

### Ошибка: "port is already allocated" (порт 5432)

VibeMeet использует порт 5432, поэтому Auth-сервис настроен на порт 5433.

Решение: уже исправлено в конфигурации!

### Ошибка: "port 8080 already allocated"

Другой процесс использует порт 8080.

Найдите процесс:
```bash
lsof -i :8080
```

Убейте процесс:
```bash
kill -9 <PID>
```

### Ошибка: 502 Bad Gateway при регистрации

Auth-сервис не запущен!

Решение:
```bash
cd /Users/ilpaka/Development/NextUp-website
./start-auth-service.sh
```

### Ошибка: Connection refused

Проверьте, что оба сервиса запущены:
```bash
# Auth-сервис
curl http://localhost:8080/health

# VibeMeet
curl http://localhost/health
```

---

## 📊 Архитектура

```
Browser (localhost)
    ↓
Nginx (localhost:80)
    ├─→ /auth-api/* → host.docker.internal:8080 (Auth-сервис)
    ├─→ /api/*      → backend:8080 (VibeMeet API)
    └─→ /*          → static files (HTML/JS/CSS)

Auth-сервис (localhost:8080)
    ↓
PostgreSQL (localhost:5433)

VibeMeet Backend (docker: backend:8080)
    ↓
PostgreSQL (docker: postgres:5432)
```

---

## ✅ Чеклист перед запуском

- [ ] Установлен Docker
- [ ] Установлен Go 1.22+
- [ ] Порт 80 свободен
- [ ] Порт 8080 свободен
- [ ] Порт 5432 свободен (для VibeMeet)
- [ ] Порт 5433 свободен (для Auth-сервиса)

---

## 🎯 Быстрая проверка всех портов

```bash
# VibeMeet PostgreSQL (должен быть занят)
lsof -i :5432

# Auth PostgreSQL (должен быть занят)
lsof -i :5433

# Nginx (должен быть занят)
lsof -i :80

# Auth-сервис (должен быть занят)
lsof -i :8080

# VibeMeet Backend (внутри Docker, проверяем так)
docker ps | grep video_conference_backend
```

---

## 🔗 Полезные ссылки

- VibeMeet: http://localhost
- Auth API: http://localhost:8080
- Auth Health: http://localhost:8080/health
- VibeMeet Health: http://localhost/health

---

**Версия:** 3.0  
**Дата:** 2026-01-15  
**Статус:** ✅ Полностью готово

🎉 **Теперь всё должно работать!** 🎉
